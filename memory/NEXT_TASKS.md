# 后续任务队列

此队列记录推荐顺序，不替代项目负责人在新会话中的最新指令。

## P0-A：恢复团队远端同步

目标：让本地 `main`、标签和 memory 能安全上传团队仓库。

验收：

- 当前 GitHub 身份拥有 `Decembered/ai-pet-conversation-agent` 写权限。
- `main` 配置远端跟踪分支。
- `git push` 和标签推送成功。
- 远端不包含 `conf.yaml`、数据库、日志、缓存和密钥。

## P0-B：自然语言喂食端到端验收

目标：验证最重要的产品纵向闭环，而不只是分别测试组件。

场景：用户说“给你吃一条鱼”。

验收：

- LLM 主动调用 `feed_pet`，而不是只用文字说吃过了。
- 同一 `request_id` 重试不会重复加状态。
- SQLite 中状态和事件一致更新。
- 页面状态自动刷新。
- Live2D 动作、显示文本和 TTS 与工具结果一致。
- 有自动化测试或可重复的 Demo 验收脚本。

## P0-C：冻结状态事件协议

状态：已在当前未提交工作树完成基础实现；待真实运行验收。

目标：把前端 HTTP 刷新升级为明确的状态变更事件。

验收：

- [x] 定义 `pet_state_changed`、`pet_reaction` 和 `ReactionPlan` schema v1。
- [x] HTTP 或独立 SQLite/MCP 写入后，连接中的页面收到 WebSocket 事件。
- [x] 前端以事件更新，重连时通过 HTTP 获取完整快照。
- [x] 添加领域、协议和 WebSocket 路由测试及版本化 memory 说明。
- [ ] 在实际运行的浏览器与 MCP 进程中完成手工验收。

## P0-C2：Live2D 原生互动渲染（交接文档 Phase A）

状态：后端解析、前端动作桥与受控音效已在当前未提交工作树完成；待真机浏览器验收。

验收：

- [x] 盘点 `mao_pro` 真实动作与表情，不虚构 `eat` 动作。
- [x] `ReactionPlan` 通过 `render` 映射到真实动作或真实降级表情。
- [x] 受控音效可静音、失败静默降级，不影响状态、气泡与 TTS。
- [x] 动作映射、降级路径和 `render` 附加逻辑有 Python 测试覆盖。
- [ ] 浏览器实测：喂食 1 秒内可见反馈、`event_id` 不重复播放、重连不重放。
- [x] 补充并注册吃/玩/清洁/睡眠动作素材；`missing_motion_assets=[]`。
- [ ] 真机调校六个程序化动作的幅度、穿模、淡入淡出和睡眠循环衔接。

## P0-C3：三种真实宠物行为（交接文档 Phase B）

状态：领域规则、MCP 工具、HTTP 路由、UI 入口、Persona 约束和测试已完成；待真机验收。

验收：

- [x] `play_with_pet` / `clean_pet` / `put_pet_to_sleep` 的领域规则与状态差量。
- [x] 独立事件类型、`request_id` 幂等、状态与事件原子写入。
- [x] 每个动作的 `ReactionPlan` 与对应粒子/音效标识。
- [x] MCP 工具、HTTP 路由和状态卡最小 UI 入口。
- [x] Persona Prompt 写明必须调用工具、不能假装成功。
- [x] 领域测试、路由测试、重复请求测试和边界裁剪测试。
- [ ] 真机验收：自然语言「陪我玩 / 给你洗澡 / 你去睡吧」触发对应 MCP 工具。
- [ ] 睡眠恢复在真实关机/重开后的表现确认。

## P0-C4：统一头顶气泡与界面布局（交接文档 Phase C）

状态：说话投影、气泡合并、定位跟随和抽屉骨架已完成；待真机验收。

验收：

- [x] 普通助手回复投影到头顶气泡，聊天历史与字幕保持可见且内容一致。
- [x] 气泡使用过滤后的 `display_text`，控制标签有服务端兜底清洗。
- [x] 定位基于 `#live2d` 画布与模型矩阵偏移提示，并做视口夹取与尺寸跟随。
- [x] 长文本展开/收起、移动端布局、`role="status"` 与 `aria-live`。
- [x] 右侧抽屉骨架：最近对话（短期缓冲）+ 共同记忆空状态。
- [ ] 真机验收：长回复、窗口缩放、拖动模型、移动端宽度下的气泡位置。
- [ ] 按 Live2D 实际可见包围盒计算头部位置（当前为启发式）。

## P0-C5：摄像头画中画与隐私状态（交接文档 Phase D）

状态：识别、画中画、形状/拖动/缩放、常驻指示与停止入口已完成；待真机验收。

验收：

- [x] 精确识别摄像头预览，排除屏幕共享与整屏背景视频。
- [x] 圆角、轻阴影、渐隐遮罩、拖动、缩放。
- [x] 16:9 / 圆形 / 隐藏预览三种模式，状态持久化。
- [x] 摄像头开启时指示灯与「正在提供画面」常驻，停止入口始终可见。
- [x] 原始帧不进入日志、数据库、memory；有隐私回归测试。
- [ ] 真机验收：开启摄像头后画中画是否出现、拖动是否跟手、停止是否真的切断画面。
- [ ] 确认停止画面后应用侧开关状态的表现（我们直接 stop 轨道，应用可能仍显示为开启）。

## P0-C6：最小生活节奏与背景变化（交接文档 Phase E）

状态：时钟、日程、行为优先级、世界事件持久化、离线摘要和动态背景已完成；待真机验收。

验收：

- [x] 一天内三个可恢复行为：休息、学习、玩耍。
- [x] `WorldClock` / `RoutineScheduler` / `BehaviorPlanner` / `WorldEvent`。
- [x] 背景只用项目内素材，缺失降级为渐变；不接联网天气。
- [x] 重启恢复当前事件；离线只生成摘要，不补播动画。
- [x] 时段、日程、优先级、恢复与摘要均有测试覆盖（`tests/test_pet_world.py` 19 项）。
- [ ] 真机验收：跨时段观察背景是否平滑切换、生活条文案是否准确。
- [ ] 确认 `backgrounds/` 在目标机器上确实存在（该目录 git-ignored）。
- [ ] 后续：把离线摘要改为按真实世界事件时间线归纳，而不是按日程推导。

## P0-D：性能可观测性

目标：区分模型、视觉、TTS 和播放各自耗时，避免凭感觉优化。

验收：

- 记录 ASR 完成、LLM 首 token、首完整句、TTS 完成、首音频发送和前端播放完成时间。
- 日志不包含用户图片、完整私密文本或 API Key。
- 分别测量文本、语音、语音+图片三种路径。
- 根据数据决定是否压缩摄像头图片、降低发送频率或更换/配置 TTS。

## P0-E：发布 `0.1.x`

目标：在上述闭环和远端同步完成后形成可复现发布。

验收：

- 从空目录克隆后可按 RUNBOOK 启动。
- 完整测试通过。
- 更新变更日志、配置说明和 Demo 脚本。
- 确认 Live2D、头像、音色等资源许可。
- 新标签包含视觉、表情隐藏、TTS 平滑和项目 memory，而不是继续使用旧 `v0.1.0` 快照。

## P1：公共契约与下一阶段

在 P0 完成后，优先冻结：

- `PetGrowth`
- `MemoryRecord`
- `WorldEvent`
- `GeneratedArtifact`
- `PerceptionEvent`
- PersonaPack schema

随后选择一条纵向切片，建议优先级：

1. 跨会话长期记忆与可解释召回。
2. 四人格原子切换。
3. 写信并生成受控本地文件。
4. 绘画、作品索引和预览。
5. 摄像头存在/微笑等低风险事件与主动调度冷却。

不要同时开启所有 P1 模块；先冻结公共对象，再逐条做可观察的端到端闭环。
